export interface VideoDTO {
  cveVideo?: number;
  cursoId?: number;
  titulo?: string;
  url?: string;
  duracion_minutos?: number;
  orden?: number;
  portada?: string; // Nombre del archivo de la portada

}
